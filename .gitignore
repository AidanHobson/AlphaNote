# dependencies
/node_modules

# build output
/client/dist

# secrets — never commit real keys
.env
.env.local
.env.*.local

# logs / os
*.log
npm-debug.log*
.DS_Store

# server-side disk cache
.insider-cache.json

# SQLite user database (accounts, sessions, per-user state) + snapshots
alphanote.db
alphanote.db-wal
alphanote.db-shm
*.db
db-backups/

# local editor / preview tooling
.claude/
